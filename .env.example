# PayToll API URL (default: http://localhost:3000)
PAYTOLL_API_URL=http://localhost:3000

# Option 1 (direct): Private key for x402 payments (required if secure options below are not used)
# Must have Base Sepolia USDC + ETH for gas
PRIVATE_KEY=0x...

# Option 2 (macOS Keychain): store key in Keychain and set service/account (recommended on macOS)
# PRIVATE_KEY_KEYCHAIN_SERVICE=paytoll-mcp
# PRIVATE_KEY_KEYCHAIN_ACCOUNT=$USER

# Option 3 (Linux Secret Service): store key in GNOME Keyring/libsecret and set service/account (recommended on Ubuntu)
# PRIVATE_KEY_SECRET_SERVICE=paytoll-mcp
# PRIVATE_KEY_SECRET_ACCOUNT=$USER

# Option 4 (advanced): command that prints private key to stdout
# PRIVATE_KEY_COMMAND=op read "op://Vault/PayToll Wallet/private key"
